# EPL v9.0 — Runtime dependencies
#
# Everything else (interpreter, type checker, REPL, transpilers, package
# manager, LSP, VM, formatter, profiler, web framework adapter, …) is pure
# Python standard library. Install only what you actually need.

# ─── Required for `epl serve` on Linux/macOS ─────────────
gunicorn>=21.2.0

# ─── Required for `epl serve` web app glue ───────────────
flask>=3.0.0

# ─── Optional: pick ONE for hardware-accelerated AES-CBC ─
# pycryptodome>=3.19.0
# cryptography>=41.0.0

# ─── Optional: production server on Windows ──────────────
# waitress>=2.1.0

# ─── Optional: ASGI server ───────────────────────────────
# uvicorn>=0.25.0

# ─── Optional: PostgreSQL connector ──────────────────────
# psycopg2-binary>=2.9.0

# ─── Optional: MySQL connector ───────────────────────────
# mysql-connector-python>=8.2.0

# ─── Optional: LLVM native compilation ───────────────────
# llvmlite>=0.41.0

# ─── Optional: Redis store/session backend ───────────────
# redis>=5.0.0

# ─── Optional: Mobile builder (BeeWare/Toga) ─────────────
# toga>=0.4.0
# briefcase>=0.3.0

# ─── Optional: Game development ──────────────────────────
# pygame>=2.5.0

# ─── Optional: ML / AI ───────────────────────────────────
# scikit-learn>=1.3.0
# joblib>=1.3.0

# ─── Optional: Data science ──────────────────────────────
# pandas>=2.0.0
# numpy>=1.24.0
# matplotlib>=3.7.0

# ─── Optional: Development (running the test suite) ──────
# pytest>=7.0
# coverage>=7.0

# All other features (interpreter, JS transpiler, Kotlin generator,
# package manager, HTML generator, web framework, type checker, linter,
# formatter, profiler, REPL, LSP, VM) use Python standard library only.
PyYAML>=6.0
pytest-cov>=4.0.0
