version: 2.1
jobs:
  build:
    docker:
    - image: python:3.9
    steps:
    - checkout
    - run:
        name: Install dependencies
        command: ''
    - persist_to_workspace:
        root: .
        paths:
        - .
  test:
    docker:
    - image: python:3.9
    steps:
    - checkout
    - attach_workspace:
        at: .
    - run:
        name: Run tests
        command: pytest tests/ -v --cov
  deploy:
    docker:
    - image: python:3.9
    steps:
    - checkout
    - run:
        name: Deploy
        command: ./deploy.sh
workflows:
  version: 2
  build_test_deploy:
    jobs:
    - build
    - test:
        requires:
        - build
    - deploy:
        requires:
        - test
        filters:
          branches:
            only:
            - main
